import { KeepAliveDirective } from './keep-alive.directive';

describe('KeepAliveDirective', () => {
  it('should create an instance', () => {
    const directive = new KeepAliveDirective();
    expect(directive).toBeTruthy();
  });
});
